<%- include("./partials/header") %>

<script>
    $(document).ready(function () {
    
      if (alert === true) {
        var alert = 'Alerta'
        var alertTitle = 'Venta creada'
        var alertMessage = 'Su venta fue registrada con exito'
        swal(alertTitle, alertMessage, "success");
      }
    
    })
    
    </script>
        <div class="text-center w-100 bg-light ">
            <div class="container text-center mx-auto">
                <h1>Punto de venta - Bazar de ropa</h1>
            </div>
        </div>
        <div class="container my-5 h-100">
            <div class="jumbotron h-90 my-auto justify-content-center align-self-center text-center bg-light">  
    

                <div class="container h-50 my-auto justify-content-center align-self-center text-center">
                    <div class="row justify-content-center">
                        <div class="col-10 align-self-center text-center">
                            <input class="form-control" type="search" placeholder="Buscar producto..." aria-label="Search">
                        </div> 
                        <div class="col-2 align-self-center text-center"> 
                            <button class="btn btn-outline-success" type="submit">Buscar</button>  
                        </div> 
                    </div>
                    <form action="/pos/save_sell" method="POST">
                        <div class="row justify-content-center mt-5">
                            <div class="col-lg align-self-center text-left">
                                <div class="col-lg">
                                    <div class="form-group">
                                        <label>Seleccionar producto</label>
                                        <select name="producto" class="form-control" placeholder="Producto...">
                                            <% if(productos) { %>
                                            <% for(var producto of productos) { %>
                                            <option value="<%= producto.id %>"><%= producto.descripcion %></option>
                                            <% } %>
                                            <% } %>
                                        </select>
                                    </div>
                                </div>
                                
                                
                            </div>
                        </div>
                        <div class="row justify-content-center mt-5">
                            <div class="col-lg align-self-center text-left">
                                <label>Seleccionar empleado</label>

                                <div class="form-group">
                                    <select name="empleado" class="form-control" placeholder="Empleado...">
                                        <% if(empleados) { %>
                                        <% for(var empleado of empleados) { %>
                                        <option value="<%= empleado.id %>"><%= empleado.nombres + " " + empleado.apellido_paterno + " " + empleado.apellido_materno %></option>
                                        <% } %>
                                        <% } %>
                                    </select>
                                </div>
                   
                            </div>

                            <div class="col-lg align-self-center text-left">
                                <label>Seleccionar numero de cliente</label>

                                <div class="form-group">
                                    <select name="numero_cliente" class="form-control" placeholder="Cliente...">
                                        <% if(clientes) { %>
                                        <% for(var cliente of clientes) { %>
                                        <option value="<%= cliente.id %>"><%= cliente.nombre + " " + cliente.apellido_paterno + " " + cliente.apellido_materno %></option>
                                        <% } %>
                                        <% } %>
                                    </select>
                                </div>
                   
                            </div>
                        </div>

                       
                        <div class="row justify-content-center mt-5 text-right">
                            <div class="col">
                                <button class="btn btn-outline-success" type="submit">Registrar venta</button>  
                            </div>

                    </form>                                      
                </div>         -
            </div>
        </div>

    </body>
</html>